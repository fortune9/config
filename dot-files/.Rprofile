# Math functions
## logarithms
myexp<-function(base,expo) exp(expo*log(base))
mylog<-function(base,N) log(N)/log(base)



.First <- function(){
#       library(Hmisc)
#    library(R2HTML)
        library(grDevices)
        setEPS()
        #.libPaths(c('~/tools/extra/R/lib',.libPaths()))
        newmar<<-c(3.0,3.0,1.1,1.1)
        newmgp<<-c(2.0,0.8,0.0)
        # cat("\nWelcome at", date(), "\n")
        message("\nWelcome at ", date(), "\n")
}

.Last <- function(){
         # cat("\nGoodbye at ", date(), "\n")
        message("\nGoodbye at ", date(), "\n")
}

# setup used for devtools
options(
  usethis.full_name = "Zhenguo Zhang",
  usethis.protocol  = "https",
  usethis.description = list(
    "Authors@R" = utils::person(
        given="Zhenguo", 
		family="Zhang",
        email = "zhangz.sci@gmail.com",
        role = c("aut", "cre"),
        comment = c(ORCID = "0000-0002-9444-9604")
    ),
    Version = "0.0.1"
  ),
  usethis.destdir = "E:/programming/R"
)
